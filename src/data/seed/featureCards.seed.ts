import type { FeatureCard } from '../types/entities'
import { COLLABORATOR_IDS } from './collaborators.seed'
import { PROJECT_ID, SEED_UPDATED_BY } from './constants'
import { daysBeforeAnchor } from './dates'
import { mkId, noteId } from './ids'

export const FEATURE_IDS = {
  editUnderFireBuilding: mkId('feature', 'edit-under-fire-building'),
  stormDamageScaling: mkId('feature', 'storm-damage-scaling'),
  squadReviveRebootVan: mkId('feature', 'squad-revive-reboot-van'),
  weaponRarityLootPool: mkId('feature', 'weapon-rarity-loot-pool'),
  materialHarvesting: mkId('feature', 'material-harvesting'),
  squadPingSystem: mkId('feature', 'squad-ping-system'),
  seasonalVaultedWeaponRotation: mkId('feature', 'seasonal-vaulted-weapon-rotation'),
  mantleSlideMovementTech: mkId('feature', 'mantle-slide-movement-tech'),
  eliminationFeedSpectateCam: mkId('feature', 'elimination-feed-spectate-cam'),
  botFillMatchmaking: mkId('feature', 'bot-fill-matchmaking'),
  adaptiveStormTensionScore: mkId('feature', 'adaptive-storm-tension-score'),
  crossPlayMatchmaking: mkId('feature', 'cross-play-matchmaking'),
} as const

export function buildFeatureCards(): FeatureCard[] {
  const base = { projectId: PROJECT_ID, updatedBy: SEED_UPDATED_BY }

  const cards: FeatureCard[] = [
    {
      ...base,
      id: FEATURE_IDS.editUnderFireBuilding,
      name: 'Edit-Under-Fire Building',
      playerPromise: 'I can throw up a wall and edit a window into it before the bullets land.',
      summary: 'Instant piece placement plus a fast edit window (door, window, stair) for cover you shape mid-firefight.',
      logic: 'Consumes buildMaterialCost from the matching material pool, gated by buildEditWindowMs for edits. See the SYSTEM AND CORE vault folder for the full placement state machine.',
      dependencies: [],
      logicNoteIds: [noteId('buildPieceHealth'), noteId('buildEditWindowMs'), noteId('buildMaterialCost'), noteId('buildPieceType')],
      status: 'shipped',
      owner: COLLABORATOR_IDS.deshawn,
      risk: 'low',
      createdAt: daysBeforeAnchor(145),
      updatedAt: daysBeforeAnchor(40),
    },
    {
      ...base,
      id: FEATURE_IDS.stormDamageScaling,
      name: 'Storm Damage Scaling',
      playerPromise: 'The storm is never a safe place to wait out a fight.',
      summary: 'Damage-per-tick and radius both scale with the match phase, so late circles are always more dangerous than early ones.',
      logic: 'Reads stormPhaseIndex to drive stormRadiusCurrent and stormDamagePerTick together — see WORLD.',
      dependencies: [],
      logicNoteIds: [noteId('stormRadiusCurrent'), noteId('stormDamagePerTick'), noteId('stormPhaseIndex')],
      status: 'in-build',
      owner: COLLABORATOR_IDS.malik,
      risk: 'medium',
      createdAt: daysBeforeAnchor(140),
      updatedAt: daysBeforeAnchor(38),
    },
    {
      ...base,
      id: FEATURE_IDS.squadReviveRebootVan,
      name: 'Squad Revive & Reboot Van',
      playerPromise: "Losing a fight doesn't have to mean losing my match.",
      summary:
        'Design calls for a reboot card that expires 90 seconds after an elimination, so recovering a teammate is a real timing decision. The build never expires the card at all — squads can reboot arbitrarily late, exactly the kind of drift a live game can\'t ship with.',
      logic: 'rebootCardActive gates the pickup prompt; rebootCardExpiresAt should but currently does not enforce the 90s window. See the Chrono-Parry-style divergence note on rebootCardExpiresAt.',
      dependencies: [],
      logicNoteIds: [noteId('rebootCardActive'), noteId('rebootCardExpiresAt'), noteId('squadDownedCount')],
      status: 'in-build-diverged',
      owner: COLLABORATOR_IDS.deshawn,
      risk: 'high',
      createdAt: daysBeforeAnchor(150),
      updatedAt: daysBeforeAnchor(12),
    },
    {
      ...base,
      id: FEATURE_IDS.weaponRarityLootPool,
      name: 'Weapon Rarity & Loot Pool',
      playerPromise: 'Every pickup tells me at a glance whether it\'s worth the detour.',
      summary: 'Five-tier color-coded rarity system driving the weighted drop table across floor loot, chests, and supply drops.',
      logic: 'lootPoolWeights determines spawn odds per weaponRarityTier.',
      dependencies: [],
      logicNoteIds: [noteId('lootPoolWeights'), noteId('weaponRarityTier')],
      status: 'shipped',
      owner: COLLABORATOR_IDS.priya,
      risk: 'low',
      createdAt: daysBeforeAnchor(148),
      updatedAt: daysBeforeAnchor(45),
    },
    {
      ...base,
      id: FEATURE_IDS.materialHarvesting,
      name: 'Material Harvesting',
      playerPromise: 'Anything I can see, I can probably break down for parts.',
      summary: 'Swing-based harvesting of wood, stone, and metal from the environment, feeding straight into the building economy.',
      logic: 'harvestYieldRate feeds playerMaterialWood and its siblings; depends on Edit-Under-Fire Building for the material sinks to mean anything.',
      dependencies: [FEATURE_IDS.editUnderFireBuilding],
      logicNoteIds: [noteId('harvestYieldRate'), noteId('playerMaterialWood'), noteId('playerMaterialStone'), noteId('playerMaterialMetal')],
      status: 'shipped',
      owner: COLLABORATOR_IDS.deshawn,
      risk: 'low',
      createdAt: daysBeforeAnchor(148),
      updatedAt: daysBeforeAnchor(55),
    },
    {
      ...base,
      id: FEATURE_IDS.squadPingSystem,
      name: 'Squad Ping System',
      playerPromise: 'I can call out loot, enemies, or a rotation plan without saying a word.',
      summary: 'Context-sensitive marker system readable by the whole squad, no voice chat required.',
      logic: 'pingActiveMarker drives the shared world-space marker; pingCooldownSeconds throttles spam.',
      dependencies: [],
      logicNoteIds: [noteId('pingActiveMarker'), noteId('pingCooldownSeconds')],
      status: 'in-build',
      owner: COLLABORATOR_IDS.malik,
      risk: 'low',
      createdAt: daysBeforeAnchor(90),
      updatedAt: daysBeforeAnchor(20),
    },
    {
      ...base,
      id: FEATURE_IDS.seasonalVaultedWeaponRotation,
      name: 'Seasonal Vaulted Weapon Rotation',
      playerPromise: 'The meta actually changes every season, not just the cosmetics.',
      summary: 'Weapons cycle out of the active loot pool each season without deleting them from the game, ready to return later.',
      logic: 'vaultedWeaponList zeroes a weapon\'s weight in lootPoolWeights without touching inventories that already hold it.',
      dependencies: [FEATURE_IDS.weaponRarityLootPool],
      logicNoteIds: [noteId('vaultedWeaponList'), noteId('seasonNumber')],
      status: 'designed',
      owner: COLLABORATOR_IDS.priya,
      risk: 'medium',
      createdAt: daysBeforeAnchor(70),
      updatedAt: daysBeforeAnchor(18),
    },
    {
      ...base,
      id: FEATURE_IDS.mantleSlideMovementTech,
      name: 'Mantle & Slide Movement Tech',
      playerPromise: 'I can move through a fight, not just around it.',
      summary: 'Ledge mantling and slide-cancel tech that rewards momentum over standing still and trading shots.',
      logic: 'mantleLedgeCheckRange gates the mantle probe; slideSpeedMultiplier governs the slide speed curve.',
      dependencies: [],
      logicNoteIds: [noteId('mantleLedgeCheckRange'), noteId('slideSpeedMultiplier')],
      status: 'in-build',
      owner: COLLABORATOR_IDS.deshawn,
      risk: 'medium',
      createdAt: daysBeforeAnchor(65),
      updatedAt: daysBeforeAnchor(22),
    },
    {
      ...base,
      id: FEATURE_IDS.eliminationFeedSpectateCam,
      name: 'Elimination Feed & Spectate Cam',
      playerPromise: 'Even after I\'m out, I can still see how my squad\'s story ends.',
      summary: 'Live top-left elimination feed for everyone, plus a full spectate camera for eliminated players.',
      logic: 'eliminationFeedEntry populates the feed; spectateCamTargetIndex drives which teammate the camera follows.',
      dependencies: [],
      logicNoteIds: [noteId('eliminationFeedEntry'), noteId('spectateCamTargetIndex')],
      status: 'shipped',
      owner: COLLABORATOR_IDS.priya,
      risk: 'low',
      createdAt: daysBeforeAnchor(148),
      updatedAt: daysBeforeAnchor(50),
    },
    {
      ...base,
      id: FEATURE_IDS.botFillMatchmaking,
      name: 'Bot-Fill Matchmaking',
      playerPromise: 'A quiet server at 3am still gets me into a full match fast.',
      summary: 'Backfills a slow-to-fill lobby with practice bots below a queue-size threshold, real-player modes only.',
      logic: 'matchmakingBotFillThreshold gates when bots are added; never applies to ranked queues.',
      dependencies: [],
      logicNoteIds: [noteId('matchmakingBotFillThreshold')],
      status: 'idea',
      owner: COLLABORATOR_IDS.deshawn,
      risk: 'medium',
      createdAt: daysBeforeAnchor(15),
      updatedAt: daysBeforeAnchor(6),
    },
    {
      ...base,
      id: FEATURE_IDS.adaptiveStormTensionScore,
      name: 'Adaptive Storm Tension Score',
      playerPromise: 'The music tells me when the circle is about to make my next call a lot harder.',
      summary: 'Tension-scored music layering driven by storm phase, squad count, and player health.',
      logic: 'stormTensionScore formula feeds musicLayerIntensity crossfades.',
      dependencies: [FEATURE_IDS.stormDamageScaling],
      logicNoteIds: [noteId('stormTensionScore'), noteId('musicLayerIntensity')],
      status: 'idea',
      owner: COLLABORATOR_IDS.sofia,
      risk: 'low',
      createdAt: daysBeforeAnchor(12),
      updatedAt: daysBeforeAnchor(9),
    },
    {
      ...base,
      id: FEATURE_IDS.crossPlayMatchmaking,
      name: 'Cross-Play Matchmaking',
      playerPromise: 'Was going to let squads queue together regardless of platform.',
      summary: 'Cut from v1 scope to protect the ship window and keep input-fairness questions off the table for launch — see the decision log.',
      logic: 'No implementation exists; cut before any logic notes were written for it.',
      dependencies: [],
      logicNoteIds: [],
      status: 'cut',
      owner: COLLABORATOR_IDS.priya,
      risk: 'low',
      createdAt: daysBeforeAnchor(120),
      updatedAt: daysBeforeAnchor(60),
    },
  ]

  return cards
}
